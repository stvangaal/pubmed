# contracts.yaml — version pins for contract test enforcement
#
# Must stay in sync with:
#   1. Each spec's Requires/Provides tables (the human-readable pins)
#   2. Each definition's ## Version header (the canonical version)

specs:
  traverser:
    requires:
      definitions/ruleflow-schema.md: v1
    provides:  # Primary implementor — types.ts is the canonical expression (see REGISTER.md)
      definitions/ruleflow-schema.md: v1
  analyzer:
    requires:
      definitions/ruleflow-schema.md: v1
  mermaid-generator:
    requires:
      definitions/ruleflow-schema.md: v1
